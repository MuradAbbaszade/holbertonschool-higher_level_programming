#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_matrix = []
    for i, array in enumerate(matrix):
        for j, value in enumerate(array):
            new_matrix.append(value ** 2)
    return new_matrix
